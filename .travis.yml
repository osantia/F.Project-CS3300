language: ruby
cache: bundler
bundler_args: --without production

rvm:
 - 2.7.0
 
before_install:
 - gem install bundler:2.2.11

script:
 - bundle exec rspec
 
deploy:
 provider: heroku
 api_key: lol
 app: quiet-sands-90716
 run: "rake db:migrate"
 on:
  all_branches: true
